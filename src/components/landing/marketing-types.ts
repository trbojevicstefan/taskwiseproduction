export type MarketingNavItem = {
  label: string;
  href: string;
};

export type MarketingFlowStep = {
  title: string;
  description: string;
};

export type MarketingCard = {
  name: string;
  title: string;
  description: string;
  href?: string;
};
